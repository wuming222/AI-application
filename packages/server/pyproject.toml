[project]
name = "ai-app-server"
version = "0.0.0"
description = "LLM proxy server for AI App Gen"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "httpx>=0.28",
    "python-dotenv>=1.0",
    "websockets>=13.0",
]
