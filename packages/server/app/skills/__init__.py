"""skill 通道 = 知识层（技能目录与正文），与 MCP 那条连接层相对。

只读。两家来源由 `catalog.py` 合成一份：随仓库分发的内置技能（`local.py` + `local_skills/`）与
百炼 Managed Agents 的技能接口（`bailian.py`）。此前那条匿名的 agentexplorer 目录已于 09-19 撤掉。
取值的理由都写在 `catalog.py` 顶部。
"""
