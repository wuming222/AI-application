# 2026-09-15 需求记录

## Skill 触发问题排查

### 问题描述
项目中有对应的 skill，但一直无法触发。需要排查原因并修复。

### 可能的原因
1. Skill 注册路径或命名问题
2. 触发条件/关键词配置不正确
3. Skill 描述不够清晰，系统无法匹配用户意图
4. 权限或加载问题

### 待办
- [x] 检查已安装的 skill 列表
- [x] 确认目标 skill 的触发条件和描述
- [x] 验证 skill 是否正确注册到全局或项目级别
- [x] 测试触发场景，记录失败现象
- [x] 修复后重新验证

根因：skill 未落到 `~/.qoder-cn/skills/<name>/SKILL.md`。补齐全局注册路径后当轮即可触发。

## Markdown 解析支持

### 需求描述
当前项目需要支持 Markdown 解析功能，能够正确渲染和显示 Markdown 格式的内容。

### 使用场景
- 聊天记录中的 Markdown 消息渲染
- 文档预览
- 用户输入的富文本显示

### 技术选型
已确定使用 `react-markdown` 作为 Markdown 解析库。

### 待办
- [x] 安装 `react-markdown` 依赖
- [x] 集成到前端项目中
- [ ] 测试常见 Markdown 语法（标题、列表、代码块、链接、图片等）
- [x] 确保样式与现有 UI 协调

## 其他待办事项

（在此记录今天提出的其他需求）

## 会话列表拖动排序

### 需求描述
侧边栏的会话列表应该支持拖动排序功能，用户可以自定义会话的显示顺序。

### 使用场景
- 将重要会话拖到顶部方便快速访问
- 整理杂乱的会话列表
- 按项目或主题分组排列会话

### 待办
- [x] 调研 React 拖动库（如 @dnd-kit/core、react-beautiful-dnd 等）
- [x] 实现会话列表项的拖动功能
- [x] 保存用户自定义的排序（本地存储或后端）
- [x] 确保拖动交互流畅，有视觉反馈

## 侧边栏宽度可调整

### 需求描述
当前侧边栏宽度固定为 260px，用户无法根据实际需要调整其宽度。需要在侧边栏右侧边缘添加可拖动的调整手柄，用户可以左右拖动来改变侧边栏的宽度。

### 使用场景
- 会话标题较长时，调宽侧边栏以完整显示
- 需要更多空间查看聊天内容时，调窄侧边栏
- 不同屏幕尺寸下灵活适配布局

### 技术要求
- 在侧边栏右边缘添加拖动手柄（宽度约 4-6px）
- 支持左右拖动调整宽度
- 设置最小宽度（如 200px）和最大宽度（如 500px）
- 可选：保存用户调整的宽度偏好到 localStorage

### 待办
- [x] 在 Sidebar 组件右侧边缘添加拖动手柄
- [x] 实现鼠标拖动调整宽度的逻辑
- [x] 限制最小/最大宽度范围
- [x] 添加拖动时的视觉反馈（光标变化、高亮等）
- [x] 可选：持久化用户设置的宽度

## AI 消息 Markdown 换行过多

### 问题描述
对话里的 AI 消息渲染后空白行明显偏多，段落之间像被撑成了两三个空行，长回答读起来很松散。

### 排查记录
已定位到根因：消息气泡 `MessageList.tsx:168` 上有 inline `whiteSpace: 'pre-wrap'`，该属性会继承进 `.message-markdown` 子树。Markdown 已经用 `<p>` / `<li>` 等块级元素排好版，而源文本里原有的 `\n` 又被 `pre-wrap` 额外渲染了一次，于是每个段落之间叠加出多余空行。

从本地库取样的真实 assistant 消息（含 `\n\n` 分段 + 软换行）确认了这一叠加效应。

### 技术要求
- `.message-markdown` 内恢复由 Markdown 自身控制空白（`white-space: normal`），段落间距只由 CSS 的 margin 决定
- 工具执行气泡（`ToolGroupBubble`，渲染的是原始文本、不走 Markdown）必须继续保留 `pre-wrap`，不能一起改掉
- 代码块 `<pre>` 的空白行为不受影响

### 待办
- [x] 去掉 Markdown 容器继承到的 `pre-wrap`
- [x] 复核段落、列表、标题的间距是否符合预期
- [ ] 复核代码块、表格的间距 —— 当前会话数据里没有这两类样例，未实测
- [x] 确认工具气泡与用户消息的原始文本换行没有被改坏（工具气泡不带 `.message-markdown`，代码层确认）

## 会话标题点击出现文本光标

### 问题描述
点击侧边栏的会话标题会出现文本光标/选区，看起来像是用 `Input` 渲染的。会话标题在默认状态下应该是只读展示，点击只用于切换会话，重命名只能通过「⋯ → 重命名」进入编辑态。

### 技术要求
- 会话项默认为纯文本展示，禁止文本选中带来的光标与拖选
- 只有进入重命名编辑态时才出现可聚焦、可编辑的输入框
- 不影响点击切换会话、也不影响下一条需求里的拖动

### 待办
- [x] 确认当前光标出现的具体来源 —— 实测标题是普通 `div`（`isInput: false`），不是输入框，来源是文本可选中带来的选区与插入点
- [x] 让会话项默认不可选中文本
- [ ] 验证编辑态输入框仍能正常聚焦、改名、回车提交 —— 规则已加，in-app 浏览器视口隐藏、无法用真实指针打开菜单，未实测

## 会话列表项去掉前置图标并改为整项长按拖动

### 问题描述
1. 会话列表每一项前面的消息气泡图标（💬 样式）不需要，去掉，只留标题文本。
2. 当前拖动必须由左侧那个独立的拖拽手柄图标触发，交互太别扭。期望是直接按住会话条目本身拖动即可，不需要先瞄准一个小图标。

### 使用场景
- 按住任意一条会话标题往上下拖，即可调整顺序
- 快速点击（未按住）条目仍然只是切换会话，不会误触发拖动

### 技术要求
- 移除列表项前置图标
- 移除独立拖拽手柄，拖拽监听挂到整条会话项上
- 拖动激活方式改为长按（按住一小段时间后才进入拖拽），避免与点击切换冲突
- 点击切换、重命名菜单、删除菜单等原有交互不能被拖拽监听吞掉

### 待办
- [x] 去掉会话项前置的 `MessageOutlined` 图标
- [x] 把 dnd-kit 的 listeners 从手柄移到整条会话项
- [x] 拖动传感器改为长按激活（带容差，允许轻微抖动）
- [x] 处理列表项内按钮（⋯ 菜单）与拖拽监听的事件冲突
- [x] 浏览器实测：长按可拖、轻点可切换
- [ ] 浏览器实测：重命名与删除仍正常 —— 需要真实指针打开菜单，未实测

## 预览 / 代码切换（顺带产物下载）

### 需求描述
右侧现在只有 iframe 预览，看不到 agent 到底写了什么代码，也没法把生成的东西带出去。加一个「预览 | 代码」切换：代码视图列出当前会话 workspace 里的文件，可选中查看某个文件的内容；在这个视图里放一个下载按钮，把当前产物导出为文件。

这两件事其实是同一个需求的两个端点：文件本来就存在 `workspaceStore` 里，切换只是把它摊开；导出则是在摊开之后加一个下载动作。

### 使用场景
- 生成结果不对时，先看代码定位是哪个文件写错了，而不是在预览里瞎猜
- 想把生成的单页应用拿去别处跑，直接下载
- 演示时能直观展示"agent 真的写出了这些文件"

### 技术要求
- 数据源：`useWorkspaceStore.getState().getCurrentFiles()`，按会话隔离，切会话时要跟着换
- 复用 `PreviewArea` 的头部区域做 tab，不新开一层容器
- 代码视图**只读**。允许手改就会和下一轮 `write_file` 冲突，需要额外的覆盖规则，本次不做
- 不引入编辑器/语法高亮依赖（monaco、shiki 之类），先用 `<pre>` 展示 + 现有代码字体样式
- 下载走 Blob；`buildSrcdoc.ts` 已把多文件内联成单个 `index.html`，导出复用这个产物，不要另起一套拼装逻辑
- 空态：一个文件都没有时给出提示，而不是一片空白面板

### 待办
- [x] 头部加「预览 / 代码」两个 tab，来回切换不丢预览状态（实测 iframe 为同一 DOM 节点，不会重新加载）
- [x] 代码视图：文件列表（路径 + 大小）+ 只读内容区
- [x] 跟随会话切换刷新文件集合
- [x] 下载当前产物（index.html）—— 实测 Blob 内容与 iframe 的 srcdoc 逐字符相等
- [x] 空态提示"还没有生成文件"；无 index.html 时下载按钮禁用
- [ ] 超长文件展示 —— 200k 字符截断已实现，无真实大文件可测

### 同日调整
- [x] 文件列表去掉字符数，只留路径
- [x] 代码视图加语法高亮：`highlight.js/lib/common` 子集 + github 主题，逻辑抽到 `src/preview/highlight.ts`，按扩展名映射语言，结果 `useMemo` 缓存。原 SDD 里"不引入高亮依赖"的约束作废
- [x] 为 `dangerouslySetInnerHTML` 的转义边界补回归测试 `src/preview/__tests__/highlight.test.ts`（这条路径的安全性完全靠 hljs 转义正文）

## 空会话下点「新建会话」不应再创建一个

### 问题描述
已经处在「新对话」界面时，再点新建会话，列表上又多出一条空「新对话」；连续点会堆出好几条一模一样的空会话。

### 复现记录
浏览器实测：会话数 7（其中 3 条是空「新对话」）时连点两次新建 → 变成 9 条，顶部连着两条空的「新对话」。

根因在 `packages/web/src/store/sessionStore.ts:63`：`createSession` 无条件 `POST /api/sessions`，而 `Sidebar.tsx` 的新建按钮直接调它，全程没有"当前会话本来就是空的，复用它"这一步判断。

### 期望行为
当前会话还没产生任何内容时，点新建等于"回到干净的输入框"，不应该新增会话；只有当前会话已经有内容（发过消息 / 生成过文件）时，才真的新建一条。

### 待办
- [x] 判断放 store：新增 `createOrReuseSession`，两处新建按钮改走它，`createSession` 保持无条件新建的语义
- [x] 定义"空"：消息数为 0 且 workspace 无文件
- [x] 处理边界：无当前会话走新建；判定只看内容不看标题；流式输出时消息数已 ≥ 1，行为与现状一致
- [x] 复用时的反馈：`composerFocusTick` 把焦点送回输入框
- [x] 浏览器实测：连点新建不再堆积空会话（DOM 与后端会话数都不变），已有内容的会话仍可正常新建

## 捕获预览运行时错误并回灌给 Agent

### 问题描述
Agent 生成完代码就撒手了：预览里报了什么错、应用是不是根本没跑起来，前端不知道，模型更不知道。用户只能自己开 DevTools，再把错误手动复述给 AI。

现状核对（本条依据）：预览链路上只有三处 catch，都不是运行时错误捕获 —— `buildSrcdoc.ts:39` 的 storage 探针（SecurityError → 内存 shim）、`chatStore.sendMessage` 的请求层 catch（网络失败提示）、`toolRegistry` 的工具错误（返回字符串给模型）。`window.onerror`、`unhandledrejection`、iframe 内的 `console.error` 一个都没接。

### 期望行为
1. 预览里出现 JS 运行时报错时，界面上看得见，而不是静默白屏
2. 一键把错误整理成修复请求送进输入框，用户看过再发
3. （可选，默认关）生成结束后自动带错误信息发起一轮修复

### 必须解决的三个难点
- **跨源**：预览 iframe 是 `sandbox="allow-scripts"` 不带 `allow-same-origin` 的不透明源，父页面读不到它的 `contentWindow`。只能靠注入脚本 + `parent.postMessage` 单向传回来，父侧必须校验消息来源，光看 `origin` 不够（不透明源的 origin 恒为 `"null"`）
- **行号归因**：多个文件被内联进同一份 srcdoc，错误行号是整个文档的行号，模型按它找不到原文件。要让 stack 能归因回原始文件路径
- **成本与循环**：错误 → 自动修 → 又报错 → 再自动修，会无上限烧 token。必须有限制与去重

### 待办
- [ ] 确认方案（见 `docs/preview-error-capture/SDD.md`）
- [ ] 注入错误桥接脚本：捕获 error / unhandledrejection，缓冲 + postMessage
- [ ] 行号归因到原始文件
- [ ] 父侧接收：来源校验 + 字段收窄 + 按会话存
- [ ] 错误条 UI 与「填入输入框」动作
- [ ] 自动修复开关（默认关）与轮次上限
- [ ] 去重 / 上限逻辑补单测

## 统一到 Ant Design 组件

### 问题描述
项目已经引了 antd，但不少界面仍是裸标签 + inline style 手写的，没有吃到组件库的能力。盘点结果（实测计数）：

| 项 | 现状 |
|---|---|
| 裸 `<button>` | 6 处：`PreviewArea.tsx` 5（预览/代码 tab、刷新、下载、文件列表行）、`ChatInterface.tsx:84` 1（图片移除角标） |
| inline style | ChatInterface 14、Sidebar 13、MessageList 12、AgentProgress 8、App 6、PreviewArea 3 |
| 已在用 | Button、Input、Empty、Dropdown、message、ConfigProvider + theme |
| 缺席 | Segmented/Tabs、Tooltip（现在用 `title` 属性）、Space/Flex、Layout、Typography |

`main.tsx` 已经配了 `colorPrimary: '#6b9fd4'` 与跟随系统的 `darkAlgorithm`，但组件里散落硬编码色值 —— 一旦切到深色模式，这些写死的颜色会和 antd 的暗色表面打架。

### 本轮范围（已确认：只做 P0 + P1）
- **P0 定规范**：写进 `AGENTS.md` —— 状态/布局样式归 `.css` class，JSX 只留真正动态的值；色值、圆角、间距取 antd token，不写死。理由是先后顺序很关键：antd 组件自带 CSS-in-JS 样式，留着 inline style 去套组件会踩优先级覆盖（`.dragging` 被 inline `background` 盖掉就是这么踩的）
- **P1 换控件**：6 个裸按钮逐个判定该换成什么；`title` 属性 → `Tooltip`；预览/代码切换 → `Segmented`

### 后续追加：P2 与 P3 也做了
原计划推迟，随后按要求一并完成，细节见 `docs/antd-consistency/SDD.md` 的「P2 + P3 追加执行」。

- **P2 色值换 token**：机制不是 `var(--ant-*)`（实测这套 antd v6 在组件节点上取不到 `--ant-*` 变量），而是 `App.tsx` 用 `theme.useToken()` 桥出 `--app-*` 挂根节点，各组件 `.css` 用 `var(--app-*)`。阴影与叠在特定底色上的半透明白/黑保留字面值
- **P3 布局收敛**：没有换成 `Flex`/`Space`/`Layout`（那只是把 inline style 挪进 props，不解决状态写不进 class 的问题），而是把布局与状态样式全部收进同名 `.css`。inline style 从 56 处降到 3 处动态值
- **验证方式**：因没有视觉回归测试且 in-app 浏览器截不了图，改为改动前用 `getComputedStyle`/`getBoundingClientRect` 打基线、改完跑同一段探针比对 —— 结论是几何零变化，颜色仅按映射表位移
- **残留风险**：深色模式的实际观感没逐屏确认过（需要把系统切到 dark 再看）

### 待办
- [x] P0：规范写进 `AGENTS.md` 的编码约束（样式归属 / token 取值 / 优先用组件库三条）
- [x] 预览/代码两个裸 tab → antd `Segmented`，手写的 `.preview-tab` 四段样式已删
- [x] 刷新 / 下载 → antd `Button` 图标化（默认带边框、32×32 与 Segmented 同高），说明文字进 `Tooltip`；中间一度回退成原生文字按钮，又按要求撤回图标化版本。踩点记录在 SDD：`Button` 会给两字中文标签插字距、`type="text"` 没有轮廓所以显淡
- [x] Sidebar 的 `title="新建会话"` / `title="收起侧边栏"` → `Tooltip`（折叠态原本没提示，一并补上）；实测 `.ant-tooltip` 渲染、原生 `title` 已移除
- [x] 剩余裸按钮逐个判定并就地注明理由：文件列表行、图片移除角标、刷新、下载；角标的 inline style 收进新建的 `ChatInterface.css`
- [x] `AGENTS.md` 的组件优先条款据本轮结果校准：antd 不是无条件更优，视觉取舍以实际效果为准，保留原生需注明理由
- [x] 浏览器实测无明显尺寸回归（header 45px、Segmented 32×104px 在 280px 面板内不溢出），切视图 iframe 仍不重载
- [ ] 图片移除角标的视觉一致性未实测 —— 要真实上传文件才会渲染出角标
- [x] P2：组件 TSX 内无写死色值，面/边框/文字/圆角全部经 `--app-*` 桥接取 antd token
- [x] P3：布局与状态样式收进同名 `.css`，inline style 56 → 3 处；顺带清掉媒体查询里的 `!important` 与 JSX 内的 `@keyframes`
- [x] 改前/改后计算样式比对：几何零变化，颜色只按映射表位移；聊天列拖宽与预览保活均正常
- [ ] 深色模式观感未逐屏确认 —— 需要把系统切到 dark 再看一轮

## 样式集中：建立设计令牌层

### 需求描述
样式设置散得厉害：没有尺度体系，同类值各处各写一份，收敛到什么档位全靠临时决定。这轮把"值"从组件里抽出来，集中成一层设计令牌。

现状量化（实测）：

| 维度 | 现状 |
|---|---|
| 全局样式文件 | **不存在**。只有 `index.html` 里一行内联 `<style>`（reset + html/body 高度） |
| CSS 文件 | 7 个 / 834 行，无 tokens 文件 |
| 圆角 | 8 种取值：1 / 3 / 6 / 8 / 12 / 16 / 18 / 20 px |
| 间距 | 8 档：2 / 4 / 6 / 8 / 10 / 12 / 16 / 40 px |
| 字号 | 6 档含半档：12 / 13 / 13.5 / 14 / 15 / 18 px |
| 重复 | 等宽字体声明在 2 个文件各写一遍；同族气泡阴影写了 3 遍；圆形值 `50%` 写了两遍 |

### 已澄清的范围
1. **只建令牌层**：不引 CSS Modules / Tailwind，不做多主题切换
2. **一套语义化 scale**：`--space-*`（4px 基准）、`--radius-*`、`--font-*`、`--shadow-*`、`--motion-*`、`--font-mono`
3. **允许对齐到档位**：值吸附到 scale（如 3→4、6→8、16/20→18、13.5→13），因此会有肉眼可见的细微变化，需给出逐项 diff 清单复核
4. **一次性替换 + 样式比对**：改前用 `getComputedStyle`/`getBoundingClientRect` 打基线，改完跑同一段探针出对比报告

### 一条硬约束
颜色令牌**不能**搬进静态 `tokens.css`：它们必须运行时从 `theme.useToken()` 取，否则深色模式又退回写死值。所以分两层——
- 静态尺度（space / radius / font / shadow / motion / 字体族）→ `src/styles/tokens.css`
- 主题派生颜色 → 继续留在 `App.tsx` 的 `--app-*` 桥里

### 待办
- [x] 定档位表（每个 name 对应哪些现有值、谁被吸附）并确认 → 见 `docs/style-tokens/SDD.md`
- [x] 新建 `src/styles/tokens.css` 并在 `main.tsx` 引入
- [x] 7 个 CSS 文件的字面量全部改读令牌（残留的都是文件头注明的固有尺寸）
- [x] 改前基线 / 改后对比：已输出逐项 diff，非对齐项零变化
- [x] 令牌用法与两层拆分写进 `AGENTS.md`
- [x] `--app-*` 桥删掉圆角，只留颜色（避免同一值两套来源）
- [ ] 深色模式观感逐屏确认
