# 2026-09-15 需求记录

## Skill 触发问题排查

### 问题描述
项目中有对应的 skill，但一直无法触发。需要排查原因并修复。

### 可能的原因
1. Skill 注册路径或命名问题
2. 触发条件/关键词配置不正确
3. Skill 描述不够清晰，系统无法匹配用户意图
4. 权限或加载问题

### 待办
- [ ] 检查已安装的 skill 列表
- [ ] 确认目标 skill 的触发条件和描述
- [ ] 验证 skill 是否正确注册到全局或项目级别
- [ ] 测试触发场景，记录失败现象
- [ ] 修复后重新验证

## Markdown 解析支持

### 需求描述
当前项目需要支持 Markdown 解析功能，能够正确渲染和显示 Markdown 格式的内容。

### 使用场景
- 聊天记录中的 Markdown 消息渲染
- 文档预览
- 用户输入的富文本显示

### 技术选型
已确定使用 `react-markdown` 作为 Markdown 解析库。

### 待办
- [ ] 安装 `react-markdown` 依赖
- [ ] 集成到前端项目中
- [ ] 测试常见 Markdown 语法（标题、列表、代码块、链接、图片等）
- [ ] 确保样式与现有 UI 协调

## 其他待办事项

（在此记录今天提出的其他需求）

## 会话列表拖动排序

### 需求描述
侧边栏的会话列表应该支持拖动排序功能，用户可以自定义会话的显示顺序。

### 使用场景
- 将重要会话拖到顶部方便快速访问
- 整理杂乱的会话列表
- 按项目或主题分组排列会话

### 待办
- [ ] 调研 React 拖动库（如 @dnd-kit/core、react-beautiful-dnd 等）
- [ ] 实现会话列表项的拖动功能
- [ ] 保存用户自定义的排序（本地存储或后端）
- [ ] 确保拖动交互流畅，有视觉反馈
