# 2026-09-15 需求记录

## Skill 触发问题排查

### 问题描述
项目中有对应的 skill，但一直无法触发。需要排查原因并修复。

### 可能的原因
1. Skill 注册路径或命名问题
2. 触发条件/关键词配置不正确
3. Skill 描述不够清晰，系统无法匹配用户意图
4. 权限或加载问题

### 待办
- [x] 检查已安装的 skill 列表
- [x] 确认目标 skill 的触发条件和描述
- [x] 验证 skill 是否正确注册到全局或项目级别
- [x] 测试触发场景，记录失败现象
- [x] 修复后重新验证

根因：skill 未落到 `~/.qoder-cn/skills/<name>/SKILL.md`。补齐全局注册路径后当轮即可触发。

## Markdown 解析支持

### 需求描述
当前项目需要支持 Markdown 解析功能，能够正确渲染和显示 Markdown 格式的内容。

### 使用场景
- 聊天记录中的 Markdown 消息渲染
- 文档预览
- 用户输入的富文本显示

### 技术选型
已确定使用 `react-markdown` 作为 Markdown 解析库。

### 待办
- [x] 安装 `react-markdown` 依赖
- [x] 集成到前端项目中
- [ ] 测试常见 Markdown 语法（标题、列表、代码块、链接、图片等）
- [x] 确保样式与现有 UI 协调

## 其他待办事项

（在此记录今天提出的其他需求）

## 会话列表拖动排序

### 需求描述
侧边栏的会话列表应该支持拖动排序功能，用户可以自定义会话的显示顺序。

### 使用场景
- 将重要会话拖到顶部方便快速访问
- 整理杂乱的会话列表
- 按项目或主题分组排列会话

### 待办
- [x] 调研 React 拖动库（如 @dnd-kit/core、react-beautiful-dnd 等）
- [x] 实现会话列表项的拖动功能
- [x] 保存用户自定义的排序（本地存储或后端）
- [x] 确保拖动交互流畅，有视觉反馈

## 侧边栏宽度可调整

### 需求描述
当前侧边栏宽度固定为 260px，用户无法根据实际需要调整其宽度。需要在侧边栏右侧边缘添加可拖动的调整手柄，用户可以左右拖动来改变侧边栏的宽度。

### 使用场景
- 会话标题较长时，调宽侧边栏以完整显示
- 需要更多空间查看聊天内容时，调窄侧边栏
- 不同屏幕尺寸下灵活适配布局

### 技术要求
- 在侧边栏右边缘添加拖动手柄（宽度约 4-6px）
- 支持左右拖动调整宽度
- 设置最小宽度（如 200px）和最大宽度（如 500px）
- 可选：保存用户调整的宽度偏好到 localStorage

### 待办
- [x] 在 Sidebar 组件右侧边缘添加拖动手柄
- [x] 实现鼠标拖动调整宽度的逻辑
- [x] 限制最小/最大宽度范围
- [x] 添加拖动时的视觉反馈（光标变化、高亮等）
- [x] 可选：持久化用户设置的宽度

## AI 消息 Markdown 换行过多

### 问题描述
对话里的 AI 消息渲染后空白行明显偏多，段落之间像被撑成了两三个空行，长回答读起来很松散。

### 排查记录
已定位到根因：消息气泡 `MessageList.tsx:168` 上有 inline `whiteSpace: 'pre-wrap'`，该属性会继承进 `.message-markdown` 子树。Markdown 已经用 `<p>` / `<li>` 等块级元素排好版，而源文本里原有的 `\n` 又被 `pre-wrap` 额外渲染了一次，于是每个段落之间叠加出多余空行。

从本地库取样的真实 assistant 消息（含 `\n\n` 分段 + 软换行）确认了这一叠加效应。

### 技术要求
- `.message-markdown` 内恢复由 Markdown 自身控制空白（`white-space: normal`），段落间距只由 CSS 的 margin 决定
- 工具执行气泡（`ToolGroupBubble`，渲染的是原始文本、不走 Markdown）必须继续保留 `pre-wrap`，不能一起改掉
- 代码块 `<pre>` 的空白行为不受影响

### 待办
- [ ] 去掉 Markdown 容器继承到的 `pre-wrap`
- [ ] 复核段落、列表、代码块、标题的间距是否符合预期
- [ ] 确认工具气泡与用户消息的原始文本换行没有被改坏

## 会话标题点击出现文本光标

### 问题描述
点击侧边栏的会话标题会出现文本光标/选区，看起来像是用 `Input` 渲染的。会话标题在默认状态下应该是只读展示，点击只用于切换会话，重命名只能通过「⋯ → 重命名」进入编辑态。

### 技术要求
- 会话项默认为纯文本展示，禁止文本选中带来的光标与拖选
- 只有进入重命名编辑态时才出现可聚焦、可编辑的输入框
- 不影响点击切换会话、也不影响下一条需求里的拖动

### 待办
- [ ] 确认当前光标出现的具体来源（文本选中 / 多余的聚焦元素 / 输入框常驻）
- [ ] 让会话项默认不可选中文本
- [ ] 验证编辑态输入框仍能正常聚焦、改名、回车提交

## 会话列表项去掉前置图标并改为整项长按拖动

### 问题描述
1. 会话列表每一项前面的消息气泡图标（💬 样式）不需要，去掉，只留标题文本。
2. 当前拖动必须由左侧那个独立的拖拽手柄图标触发，交互太别扭。期望是直接按住会话条目本身拖动即可，不需要先瞄准一个小图标。

### 使用场景
- 按住任意一条会话标题往上下拖，即可调整顺序
- 快速点击（未按住）条目仍然只是切换会话，不会误触发拖动

### 技术要求
- 移除列表项前置图标
- 移除独立拖拽手柄，拖拽监听挂到整条会话项上
- 拖动激活方式改为长按（按住一小段时间后才进入拖拽），避免与点击切换冲突
- 点击切换、重命名菜单、删除菜单等原有交互不能被拖拽监听吞掉

### 待办
- [ ] 去掉会话项前置的 `MessageOutlined` 图标
- [ ] 把 dnd-kit 的 listeners 从手柄移到整条会话项
- [ ] 拖动传感器改为长按激活（带容差，允许轻微抖动）
- [ ] 处理列表项内按钮（⋯ 菜单）与拖拽监听的事件冲突
- [ ] 浏览器实测：长按可拖、轻点可切换、重命名与删除仍正常
