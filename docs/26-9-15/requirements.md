# 2026-09-15 需求记录

## Skill 触发问题排查

### 问题描述
项目中有对应的 skill，但一直无法触发。需要排查原因并修复。

### 可能的原因
1. Skill 注册路径或命名问题
2. 触发条件/关键词配置不正确
3. Skill 描述不够清晰，系统无法匹配用户意图
4. 权限或加载问题

### 待办
- [x] 检查已安装的 skill 列表
- [x] 确认目标 skill 的触发条件和描述
- [x] 验证 skill 是否正确注册到全局或项目级别
- [x] 测试触发场景，记录失败现象
- [x] 修复后重新验证

根因：skill 未落到 `~/.qoder-cn/skills/<name>/SKILL.md`。补齐全局注册路径后当轮即可触发。

## Markdown 解析支持

### 需求描述
当前项目需要支持 Markdown 解析功能，能够正确渲染和显示 Markdown 格式的内容。

### 使用场景
- 聊天记录中的 Markdown 消息渲染
- 文档预览
- 用户输入的富文本显示

### 技术选型
已确定使用 `react-markdown` 作为 Markdown 解析库。

### 待办
- [x] 安装 `react-markdown` 依赖
- [x] 集成到前端项目中
- [ ] 测试常见 Markdown 语法（标题、列表、代码块、链接、图片等）
- [x] 确保样式与现有 UI 协调

## 其他待办事项

（在此记录今天提出的其他需求）

## 会话列表拖动排序

### 需求描述
侧边栏的会话列表应该支持拖动排序功能，用户可以自定义会话的显示顺序。

### 使用场景
- 将重要会话拖到顶部方便快速访问
- 整理杂乱的会话列表
- 按项目或主题分组排列会话

### 待办
- [x] 调研 React 拖动库（如 @dnd-kit/core、react-beautiful-dnd 等）
- [x] 实现会话列表项的拖动功能
- [x] 保存用户自定义的排序（本地存储或后端）
- [x] 确保拖动交互流畅，有视觉反馈

## 侧边栏宽度可调整

### 需求描述
当前侧边栏宽度固定为 260px，用户无法根据实际需要调整其宽度。需要在侧边栏右侧边缘添加可拖动的调整手柄，用户可以左右拖动来改变侧边栏的宽度。

### 使用场景
- 会话标题较长时，调宽侧边栏以完整显示
- 需要更多空间查看聊天内容时，调窄侧边栏
- 不同屏幕尺寸下灵活适配布局

### 技术要求
- 在侧边栏右边缘添加拖动手柄（宽度约 4-6px）
- 支持左右拖动调整宽度
- 设置最小宽度（如 200px）和最大宽度（如 500px）
- 可选：保存用户调整的宽度偏好到 localStorage

### 待办
- [x] 在 Sidebar 组件右侧边缘添加拖动手柄
- [x] 实现鼠标拖动调整宽度的逻辑
- [x] 限制最小/最大宽度范围
- [x] 添加拖动时的视觉反馈（光标变化、高亮等）
- [x] 可选：持久化用户设置的宽度
